@model CanteenSystem.Models.Kitchen

@{
    ViewBag.Title = "Chỉnh sửa nhà ăn";
}
<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header bg-warning text-dark">
                        <h4 class="mb-0"><i class="fas fa-edit"></i> Chỉnh sửa nhà ăn</h4>
                    </div>
                    <div class="card-body">
                        @using (Html.BeginForm())
                        {
                            @Html.AntiForgeryToken()
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                            @Html.HiddenFor(model => model.KitchenId)

                            <div class="form-group">
                                @Html.LabelFor(model => model.KitchenName, htmlAttributes: new { @class = "control-label" })
                                @Html.EditorFor(model => model.KitchenName, new { htmlAttributes = new { @class = "form-control", placeholder = "Ví dụ: Nhà ăn A" } })
                                @Html.ValidationMessageFor(model => model.KitchenName, "", new { @class = "text-danger" })
                            </div>

                            <div class="form-group mt-4">
                                <input type="submit" value="Lưu thay đổi" class="btn btn-warning" />
                                @Html.ActionLink("Quay lại danh sách", "Index", null, new { @class = "btn btn-secondary ml-2" })
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
    @section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }
